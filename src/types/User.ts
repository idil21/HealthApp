export interface User {
  id: number;
  name: string;
  email: string;

  // age: number;
  // sex: number;
  // weight: number;
  // height: number;
  // healthGoals: string;
  // activityLevel: string;
  // dietPreferences: string;
  // exerciseHabits: string;
}
