import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    backgroundColor: "white",
    height: 48,
    padding: 12,
    margin: 5,
    borderRadius: 27,
  },
});
